This folder is committed build glue for the Android and iOS Rust library.

It was bootstrapped by `flutter_rust_bridge_codegen integrate` and uses its
vendored Cargokit scripts. Desktop platform declarations are intentionally
removed because MeetTrace targets Android and iOS only.
