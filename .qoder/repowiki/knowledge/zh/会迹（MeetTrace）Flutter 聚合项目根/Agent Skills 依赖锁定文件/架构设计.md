该模块仅包含一个顶层清单文件 skills-lock.json，采用 v1 版本结构：根级 version 字段声明格式版本，skills 对象以 skill 名称为键，每个条目包含 source（GitHub 仓库名）、sourceType（固定为 github）、skillPath（指向仓库内 SKILL.md 的相对路径）以及 computedHash（用于校验内容完整性）。.agents/skills/ 目录为空，说明实际 skill 内容由运行时或安装流程从指定 source 拉取，本文件仅作为依赖锁存在。