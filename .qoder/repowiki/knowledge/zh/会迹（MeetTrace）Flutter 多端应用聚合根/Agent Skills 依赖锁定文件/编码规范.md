- 每个 skill 条目统一包含 source、sourceType、skillPath、computedHash 四个字段，缺一不可。
- skillPath 始终指向对应仓库中 skills/<skill-name>/SKILL.md 的标准位置。
- sourceType 固定为 github，表明所有 skill 均从 GitHub 仓库拉取。
- computedHash 使用 SHA-256 对 skill 内容进行校验，确保锁定内容的不可变性。