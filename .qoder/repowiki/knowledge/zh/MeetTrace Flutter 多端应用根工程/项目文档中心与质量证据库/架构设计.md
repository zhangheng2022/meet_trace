文档按职责划分为五个子目录：`product/` 存放 PRD（Alpha_PRD_无登录版.md）作为 A0 级唯一事实源；`technical/` 存放端侧 SenseVoice 转录技术方案（A1 级实现契约）；`architecture/` 存放代码架构与冗余审计快照（A2 级）；`quality/` 通过 README 索引当前质量门槛与设备矩阵（A2），并以 Step_XX_*.md 形式冻结历史实现证据（A3）；`project/` 记录开发步骤与 Git 分支/Worktree 约定（A2）。根 `docs/README.md` 作为唯一导航入口，通过 Mermaid 权威链图明确冲突优先级（PRD → DESIGN/技术方案 → 质量门槛 → 历史证据），并维护完整的文档目录表、变更联动矩阵与维护检查清单。`superpowers/specs/` 为预留规格目录，当前为空。