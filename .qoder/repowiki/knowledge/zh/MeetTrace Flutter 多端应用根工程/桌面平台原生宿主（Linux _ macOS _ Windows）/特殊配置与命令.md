各平台需通过 Flutter 工具链生成宿主脚手架后再构建：Linux/Windows 使用 `cmake --build build/linux-x64/release` 等命令，macOS 通过 Xcode Workspace 或 `flutter build macos` 构建；所有平台在首次构建前需要运行 `flutter pub get` 以生成 `flutter/generated_plugins.cmake` 等文件。