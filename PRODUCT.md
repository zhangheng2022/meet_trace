# 会迹（MeetTrace）产品上下文

<!-- impeccable:product-schema 1 -->

## Platform

adaptive

## Users

主要用户是使用普通话或中英混合会议、需要快速回顾会议内容的 Android 与 iOS 个人用户。
他们可能处于临时讨论、日常工作会议、研究同步或长时间会议中，希望无需注册或稳定网络，
即可可靠留下完整录音并获得可核对的转录与总结。

用户愿意在准确率、模型体积、耗电和设备性能之间进行明确选择，但不应被迫理解底层
ASR 实现细节，也不应承担系统静默切换模型带来的结果混淆。

## Product Purpose

会迹（MeetTrace）帮助用户在 Android 手机/平板或 iPhone/iPad 上完成本地优先的会议记录闭环：

1. 无登录创建会议并持续录音。
2. 使用端侧 ASR 提供会中临时转录。
3. 录音结束后，基于完整本地音频生成最终转录。
4. 基于最终转录生成摘要、关键结论和行动项。
5. 从关键结论回到对应时间段的原文和本地音频。
6. 编辑、分享或永久删除本地会议数据。

产品成功首先意味着事实音频完整、安全且可恢复；其次才是转录速度、识别准确率和
AI 总结质量。推理变慢、积压、失败或不可用时，录音必须继续。

## Positioning

会迹不是云端会议机器人、AI 聊天工具或账号型协作平台，而是以本地事实音频为唯一
事实源的端侧会议记录工具。

其核心差异是“事实音频优先、端侧转录、结论可追溯”：会中结果可以降级或重建，
最终转录必须来自完整音频，AI 总结只能基于最终转录，关键结论必须能够定位到带
时间戳的原文与对应音频。

## Operating Context

- 用户完成首次运行资源初始化后，可以在临时会议中打开 App 直接录音，不依赖注册或持续联网。
- 长会议至少覆盖连续录音 30 分钟；ASR 积压时仍完整保存音频。
- 弱网或断网不影响录音和端侧转录。
- 首次初始化按固定 Manifest 下载并严格校验 SenseVoice 与 Silero VAD；资源未就绪前首页保持阻断。
- 当前只有一个真实可用的 SenseVoice 模型，不暴露模型等级、占位模型或自动回退。
- 首页“开始会议”直接使用设置中的全局默认模型；录音开始后，本场模型锁定。
- 新会议先使用“待生成标题”，最终转录完成后由 AI 总结生成会议标题。
- 会议结束后，用户可以查看最终转录、总结、证据、录音和处理状态，也可以重新转录、
  修订、分享或删除数据。
- 设置用于查看全局默认 SenseVoice、执行校验与修复，以及管理本地存储、隐私说明和诊断信息。

## Capabilities and Constraints

- 会迹是 Android 与 iOS 双平台自适应产品；同一业务能力在两端共享，界面结构、导航、
  系统反馈和交互细节分别遵循 Android 与 iOS 的原生惯例。
- 当前可运行实现、发布门禁和质量证据仍以 Android Alpha 为基线；iOS 端尚未形成等价的
  录音、端侧 ASR、后台生命周期、模型分发与真机验收证据，不得表述为已经交付。
- Web 和桌面端不在当前产品范围。
- Alpha 不提供登录、账号、跨设备同步、团队协作、日历机器人、自动入会或多人在线编辑。
- 本地完整音频是唯一事实源，录音写入与 ASR 推理必须独立运行。
- App 私有目录保存会议录音、转录、模型与派生数据；卸载应用可能永久删除这些数据。
- 不使用云端 ASR，不上传音频或会中临时转录，不在服务端保存音频。
- 使用云端 AI 总结时只允许上传最终文本和必要的最小片段元数据。
- 会中转录是可降级、可重建的临时结果，不得冒充最终转录。
- AI 总结只能读取已完成且当前激活的最终转录快照。
- 每条关键结论和行动项必须保留带时间戳的转录证据。
- 说话人分离、会中转录和云端 AI 总结都是可降级能力，不得阻塞事实录音。
- SenseVoice 与 Silero VAD 权重不得进入 APK/IPA；首次初始化通过固定 HTTPS Manifest
  下载、续传、校验并原子激活。当前唯一 SenseVoice 不提供删除入口。
- 设置保存全局默认模型；首页不提供本场模型覆盖，开始会议后模型锁定。
- 同一场会议所选模型同时负责会中和最终转录，不自动切换模型，不混合两个模型的输出。
- SenseVoice 或 Silero VAD 未就绪时保持初始化阻断，只提供继续下载、暂停或修复，不创建会议。
- 会后重新转录生成独立快照；新快照完整写入并校验成功后，才能替换当前结果。
- 录音、转录、摘要、证据和模型状态必须支持明确的失败说明与可恢复操作。
- Flutter UI 遵循 `View → ViewModel → Use Case / Port → Repository / Service`，功能 UI
  不直接访问 ONNX、存储或 HTTP。
- 共享界面优先使用 Forui 组件和主题令牌；平台导航、返回手势、系统权限、分享、
  后台录音与辅助功能必须保留 Android 和 iOS 各自的原生语义。

## Brand Commitments

- 中文名称是“会迹”，英文名称是“MeetTrace”；中文界面优先使用“会迹”。
- 产品语言应冷静、明确、可信，优先说明事实状态、数据是否安全以及用户下一步可以做什么。
- 不使用“出了点问题”“确定”“提交”等缺少上下文的模糊文案。
- 不把 AI 表述为事实来源，不夸大实时转录、说话人分离或总结能力。
- 后续界面设计与动效以 Emil Kowalski 的 Design Engineering 原则为明确参考：
  优秀默认值、即时反馈、稳定空间关系、克制且有目的的动效，以及完整的边缘状态处理。
- 现有视觉与交互规范由 `DESIGN.md` 维护；产品上下文不得替代或私自扩展该设计权威。

## Evidence on Hand

- `docs/product/Alpha_PRD_无登录版.md`：Android + iOS Alpha 的范围、用户流程、
  功能需求与验收标准。
- `docs/technical/端侧_SenseVoice_转录技术方案.md`：录音、初始化下载、模型锁定、存储与降级边界。
- `docs/quality/`：可靠录音、端侧 ASR、模型锁定、最终快照、证据链、数据控制和
  发布门禁的实施证据。
- `DESIGN.md`：当前多页面视觉系统、机器可读令牌、组件规范与双平台自适应规则。
- `lib/`、`test/` 与 `integration_test/`：现有实现、组件测试、领域测试和设备流程。
- `ios/`：Flutter iOS 工程壳、麦克风用途和 audio 后台模式；目前不构成 iOS 录音、
  模型、后台生命周期或真机验收已完成的证据。
- 当前没有可用于产品宣传的客户名单、用户评价、媒体报道、商业指标或公开效果声明；
  后续设计与文案不得虚构这些证明材料。
- Alpha 的 SenseVoice 固定语料评测、Android/iOS 目标 arm64 真机回归、完整验收证据及
  资源许可仍需以活动质量文档为准，不得将未闭环事项表述为已经完成。

## Product Principles

1. **事实音频不可丢。** 任何推理、网络、模型或派生能力故障都不能阻断录音持久化。
2. **结论必须可追溯。** 最终文本、摘要和行动项必须能够回到原文时间段与事实音频。
3. **选择必须明确。** 模型切换、降级、上传、删除和重新转录不得静默发生。
4. **本地能力优先。** 无登录、弱网可用、端侧转录和最小化数据外发是产品长期边界。
5. **降级仍然可信。** 能力不可用时保留事实数据、解释当前状态并提供真实的恢复路径。

## Accessibility & Inclusion

- 中文界面优先，同时正确保留 `AI`、`Markdown`、`SenseVoice`、`Silero VAD` 等必要术语。
- 状态不能只依赖颜色表达；录音、错误、积压、成功和模型状态必须同时提供图标或明确文字。
- Android 触控目标至少为 48×48 dp，iOS 触控目标至少为 44×44 pt；两端都必须保留
  系统返回语义、安全区、键盘避让和辅助技术可访问名称。
- Android 支持系统字体缩放与预测返回；iOS 支持 Dynamic Type、边缘返回手势和
  VoiceOver，不得以跨平台一致性为由覆盖这些系统行为。
- 关键流程需要覆盖两端浅色与深色模式、紧凑手机和扩展屏幕，以及至少 2.0 字体缩放，
  不得产生关键内容遮挡或横向溢出。
- 动效必须响应 Android 的“移除动画”和 iOS 的“减弱动态效果”；取消空间运动后，
  功能状态仍需即时可见。
