<!-- Hallmark · pre-emit critique: P5 H5 E4 S5 R5 V4 -->

# DESIGN.md — 会迹 · MeetTrace

> 状态：Android Alpha 多页面 UI 的唯一设计源
> 更新日期：2026-07-27
> 适用范围：会议列表、开始会议、录音、处理、结果与设置
> 设计交付：Google Stitch 静态高保真画板 → Flutter / Forui 实现

本文件锁定会迹 · MeetTrace 的交互与视觉系统。后续页面实现必须先读取本文件；
需要新增视觉语义时先更新本文件和 `lib/theme/`，不得在功能页面临时硬编码。

## 给 Google Stitch 的使用方式

本文件既是产品设计规范，也是 Stitch 的页面生成说明。生成时遵守以下顺序：

1. 先生成 `S00` 设计系统画板，锁定颜色、字体、间距、控件和交互状态。
2. 第一批生成 `S01`～`S10`，覆盖从会议列表到最终结果的完整主流程。
3. 第二批生成 `S11`～`S16`，覆盖编辑、音频、设置与异常状态。
4. 最后生成 `T01`～`T03`，验证 1024 px 宽屏结构；不是把手机画板等比拉宽。
5. 全部画板使用相同的组件、图标、字体角色、圆角和状态语言。
6. 第一轮以 390×844 浅色 Android 画板为基准，不添加设备外框。
7. 关键画板另外提供深色版本：`S05` 录音、`S09` 总结、`S13` 设置。
8. 页面文案优先使用本文给出的中文原文，不替换为 Lorem ipsum。

### 可直接粘贴到 Stitch 的全局提示词

```text
Design a production-ready Android productivity app named “会迹 · MeetTrace”.
It is a local-first meeting recorder and transcription workbench, not a SaaS
dashboard, social app, AI chatbot, or marketing page.

The visual character is calm, precise, trustworthy, restrained, and technical.
Use a modern-minimal Cobalt design system with neutral paper surfaces, thin rules,
high text contrast, compact 8–10 px radii, Lucide-style outline icons, and a 4 pt
spacing grid. Blue is reserved for primary actions and selection. Red is reserved
for active recording, destructive actions, and real errors. Amber means degraded
ASR or storage risk. Green means safely saved or verified.

The product priority is always:
local fact audio safety → meeting state → primary action → live transcript →
AI assistance.

Follow Emil Kowalski’s design-engineering principles: excellent defaults, instant
press feedback, stable spatial layout, subtle and purposeful transitions, fast
perceived performance, no ornamental motion, no animation that delays a frequent
action, and careful handling of edge states.

Use real Chinese interface copy from this specification. Design for 390×844
Android portrait first. Do not add login, account avatars, cloud sync, bottom
navigation, AI chat, illustrations, gradients, glassmorphism, floating navigation,
decorative waveform data, oversized hero sections, excessive pill badges, or
nested cards.
```

## Design Engineering 原则

本项目以 Emil Kowalski 的 Design Engineering 思想为主要设计方法，Forui 是后续
Flutter 实现基座，不限制 Stitch 的视觉表达。设计质量来自大量一致且不打扰用户的
细节，而不是单个醒目的视觉效果。

### 好默认值优先

- 页面使用一套可靠默认布局，不为相同问题提供多种视觉变体。
- 主操作、状态反馈、危险操作和空白状态在所有页面保持同一种交互语言。
- 控件默认状态应足够完整，不依赖页面临时补充颜色、阴影或动画。
- 边缘情况在组件内部被自然处理：长标题、无转录、模型不可用、ASR 积压、
  存储不足、重复点击和字体放大。

### 动效决策顺序

每个动效必须依次回答：

1. 用户一天会看到多少次？高频操作应即时响应，尽量不做位移动画。
2. 动效是否用于反馈、解释状态、维持空间关系或避免突变？
3. 是否可以被连续操作中断并自然转向？
4. 在录音和 ASR 高负载时是否仍然流畅？

只因为“看起来很酷”而存在的动效不进入产品。页面切换、列表浏览、暂停和恢复等
高频行为不得等待动画完成。

### 动效基线

| 场景 | 时长 | 行为 |
|---|---:|---|
| 按压反馈 | 100–140 ms | 轻微缩放至 0.98 后快速恢复 |
| 图标、标签状态变化 | 140–180 ms | `ease-out`，不移动布局 |
| Tooltip、小型浮层 | 125–180 ms | 从触发位置轻微淡入 |
| Tab、内容区切换 | 180–220 ms | 可中断的淡入与轻微位移 |
| Dialog、Bottom Sheet | 200–280 ms | 快速进入，退出比进入快约 20% |
| 持续进度 | 连续 | `linear`，不伪造完成百分比 |

- 不使用慢启动的 `ease-in`。
- 不从 `scale(0)` 出现；需要缩放时从 0.95 或更高开始并结合透明度。
- 高频列表不使用逐项 stagger 入场。
- 减少动画模式保留颜色和透明度反馈，取消空间位移。
- 录音期间优先使用 transform、opacity 和颜色变化，避免频繁改变布局尺寸。

## 设计语境

- 受众：使用中文记录会议、关心录音完整性和结果可信度的 Android 用户。
- 单一主任务：可靠地开始、持续并结束一场会议录音。
- 基调：克制、技术、冷静。
- 设计承诺：事实音频的安全状态永远比 ASR、模型和 AI 总结更醒目。

## 类型与结构

- Genre：`modern-minimal`。
- Theme：经 Flutter 与中文离线场景调整的 `Cobalt`。
- App macrostructure：`Workbench`。
- 内容页：连续文档结构，以阅读为主，不做装饰性分区。
- 应用页不使用插画、渐变、玻璃拟态、假设备边框或装饰性动画。
- 页面保持同一主题、字体角色、间距、圆角和交互语言。

## 信息架构

```text
会议列表
  ├─ 开始会议 → 权限与模型预检 → 录音 → 结束确认 → 最终处理 → 会议结果
  └─ 设置
       ├─ 默认模型
       ├─ 高级模型管理
       └─ 存储、隐私与诊断
```

不增加底部导航。会议是唯一主要工作区，设置保持次级入口。

## 色彩

色彩源使用 OKLCH 定义；Flutter 中使用下表预计算的 ARGB 值。页面只通过
`context.theme.colors` 和 `AppColors` 使用这些颜色。

### 浅色

| 语义 | OKLCH | Flutter ARGB |
|---|---|---|
| paper | `oklch(98.5% 0.004 250)` | `0xFFF8FAFD` |
| paper raised | `oklch(96.5% 0.008 250)` | `0xFFF0F4F9` |
| ink | `oklch(24% 0.020 258)` | `0xFF192029` |
| ink secondary | `oklch(34% 0.018 257)` | `0xFF323841` |
| rule | `oklch(88% 0.012 250)` | `0xFFD2D8DF` |
| muted | `oklch(94% 0.008 250)` | `0xFFE7ECF0` |
| muted ink | `oklch(44% 0.018 257)` | `0xFF4C535D` |
| accent | `oklch(56% 0.200 256)` | `0xFF006FE6` |
| recording | `oklch(56% 0.200 25)` | `0xFFD02B31` |
| warning | `oklch(72% 0.150 75)` | `0xFFDA950B` |
| success | `oklch(52% 0.140 150)` | `0xFF0A7E3A` |

### 深色

| 语义 | OKLCH | Flutter ARGB |
|---|---|---|
| paper | `oklch(15% 0.012 258)` | `0xFF080B10` |
| paper raised | `oklch(19% 0.014 258)` | `0xFF10141A` |
| ink | `oklch(94% 0.008 250)` | `0xFFE7ECF0` |
| ink secondary | `oklch(80% 0.010 250)` | `0xFFB9BEC4` |
| rule | `oklch(32% 0.012 258)` | `0xFF2F3339` |
| muted | `oklch(22% 0.014 258)` | `0xFF171B21` |
| muted ink | `oklch(70% 0.012 250)` | `0xFF999FA6` |
| accent | `oklch(70% 0.160 256)` | `0xFF569FFF` |
| recording | `oklch(70% 0.160 25)` | `0xFFF2716A` |
| warning | `oklch(78% 0.120 75)` | `0xFFE4AC59` |
| success | `oklch(72% 0.110 150)` | `0xFF6FB880` |

### 使用纪律

- 蓝色只用于主操作、选择、链接和焦点，单屏面积不超过约 5%。
- 红色只表示正在录音、不可恢复的危险或明确错误。
- 琥珀表示 ASR 积压、空间风险等可降级状态，必须同时显示文字。
- 绿色表示已安全保存或校验完成，必须同时显示文字或图标。
- 不以红绿作为唯一状态信号。
- 浅色正文对比度目标不低于 7:1；正文最低 4.5:1，边界最低 3:1。

## 字体

- Alpha 不增加在线字体或额外字体资产，避免破坏离线能力和继续增加 APK。
- 标题、正文沿用 Forui 的平台字体与 Android 中文回退。
- 标题使用 roman 样式和明确字重，不使用斜体强调。
- 正文最小 14 px，常规正文 16 px，行高 1.5 左右。
- 每页最多使用五档字号。
- 计时、容量、进度和时间戳启用 tabular figures，避免数字跳动。
- 模型技术名称属于次级信息；主标签使用“标准模型”“高级模型”。

## 间距与尺寸

统一 4pt 基础比例：

| Token | 值 | 用途 |
|---|---:|---|
| `space2Xs` | 4 | 标签内部、紧密关联 |
| `spaceXs` | 8 | 图标与文字、辅助行 |
| `spaceSm` | 12 | 同组控件 |
| `spaceMd` | 16 | 页面常规间距 |
| `spaceLg` | 24 | 区段间距 |
| `spaceXl` | 32 | 页面主区段 |
| `space2Xl` | 48 | 大区段 |

- 最小触控区域为 48×48。
- 控件基准高度为 48。
- 输入框和相邻按钮高度一致。
- 卡片使用 10 px 圆角；按钮和输入使用紧凑圆角，不使用胶囊按钮。
- 层级主要依靠字重、间距、边框和表面亮度，不堆叠阴影。

## 响应式

响应式只读取布局约束，不读取设备型号、方向或平台字符串。

| 尺寸类 | 宽度 | 规则 |
|---|---:|---|
| compact | `< 600` | 单列，关键操作靠近底部安全区 |
| medium | `600–839` | 可双列，但每列内容宽度不得低于 280 |
| expanded | `≥ 840` | 允许主从布局，正文保持阅读宽度 |
| wide | `≥ 1024` | 工作台最大宽度 1200，不无限拉伸 |

必须验证 320、360、375、414、600、768、840 和 1024 宽度。按钮、标签、
页签和主要导航文字不得换成两行；字体缩放至 2.0 时不得出现横向溢出。

## 动效

- 默认不使用页面入场、滚动揭示、弹跳、视差或循环装饰动画。
- 允许的两类动效：100–150 ms 按压反馈；150–220 ms 状态淡入切换。
- 焦点环立即出现，不参与动画。
- 系统要求减少动态效果时，空间运动取消，功能状态仍即时更新。
- 加载器只用于真实异步等待；预计超过 10 秒时必须显示诚实进度文案。

## 交互状态

每个交互控件必须设计并测试：

1. 默认
2. 悬停（仅精确指针）
3. 焦点
4. 按下
5. 禁用
6. 加载
7. 错误
8. 成功

禁用状态必须说明原因；错误必须说明发生了什么、已知原因和下一步。成功结果
在页面中已经可见时保持安静，不额外弹出庆祝提示。

## 共享组件契约

- `AppPageBody` 统一页面安全区、水平留白和 compact、reading、wide 三类内容宽度；
  页面不再自行复制 `Center + ConstrainedBox + Padding`。
- `AppStatePanel` 只承载页面级加载、空白和错误状态。空白状态由“当前事实、用途说明、
  单一下一步”组成；无可执行回调时不渲染禁用按钮。
- `AppStatusNotice` 使用信息、录音、警告、错误、成功五种语义。每种状态必须同时包含
  Lucide 图标和明确中文标题，不允许只改变颜色。
- `AppBottomActionBar` 只承载页面关键操作，统一底部安全区、内容宽度和禁用原因说明；
  放入 `FScaffold.footer` 时必须收缩到内容高度，不能覆盖页面正文。
- 状态反馈使用 Forui 的 `FAlert`、`FProgress` 和 `FButton`；自定义部分只从
  `context.theme`、`AppColors` 和 `AppStyle` 取值。
- 页面级成功如果结果已可见，不额外弹出 toast；异步失败在原位置说明发生了什么、
  事实音频是否安全，以及下一步操作。
- 组件预览覆盖浅色、深色、320 px 与 414 px；测试另外覆盖 2.0 字体缩放和
  320、375、414、768 px 布局。

## 页面契约

### 会议列表

- “开始会议”始终是明确文本主操作，不能只剩图标。
- 状态必须展示文字和图标；失败会议提供查看原因，不在列表直接删除。

### 开始会议

- 模型选择默认折叠，不增加开始会议的必经步骤。
- 高级模型未就绪时只提供下载、明确改用标准模型或取消。
- 开始录音防重复触发，权限和空间问题在当前上下文解释。

### 录音

- 事实音频状态、计时和结束操作优先于实时转录。
- ASR 积压或失败必须明确显示“录音仍在继续”。
- 模型开始后锁定，页面没有切换入口。
- 返回键和结束按钮进入同一个结束确认流程，不静默拦截。

### 处理与结果

- 处理中展示最终转录、说话人和总结阶段，不伪造进度。
- 结果分为摘要、转录和录音三个阅读视图。
- 转录默认阅读，用户主动进入编辑模式后才显示输入控件。
- 证据跳转必须定位本地最终片段并播放对应事实音频。

### 设置

- 默认模型、高级模型管理、存储与隐私分区展示。
- 删除高级模型不改变历史会议，不静默修改默认模型。
- 诊断导出前说明字段范围，且不显示或分享本地路径。

## Stitch 输出画板清单

### 必备手机画板

| ID | 画板 | 核心状态 |
|---|---|---|
| `S00` | Design System | 浅色、深色、组件状态与动效标注 |
| `S01` | 会议列表 | 有数据，包含录音中、已完成和失败会议 |
| `S02` | 会议列表空白 | 首次使用，没有会议 |
| `S03` | 开始会议 | 标准模型已就绪 |
| `S04` | 开始会议 | 高级模型未下载 |
| `S05` | 录音工作台 | 事实音频安全写入、实时转录正常 |
| `S06` | 录音降级 | 实时转录积压或停止，录音继续 |
| `S07` | 结束确认 | 录音中的确认 Dialog |
| `S08` | 最终处理 | 录音已安全保存，最终转录处理中 |
| `S09` | 会议结果·总结 | 总结已完成并带证据 |
| `S10` | 会议结果·转录 | 最终转录阅读模式 |
| `S11` | 转录编辑与说话人 | 编辑片段、人工标签与保存 |
| `S12` | 会议结果·录音与数据 | 音频播放、分享、重新转录和删除 |
| `S13` | 设置·转录模型 | 标准模型为默认，高级模型已安装 |
| `S14` | 设置·模型下载 | 下载中、校验中、空间不足状态 |
| `S15` | 设置·存储与隐私 | 本地用量、隐私说明和诊断导出 |
| `S16` | 状态组件总览 | loading、empty、error、success、disabled |

### 必备宽屏画板

| ID | 尺寸 | 画板 |
|---|---:|---|
| `T01` | 1024×768 | 会议列表双列工作台 |
| `T02` | 1024×768 | 录音事实区与实时转录双栏 |
| `T03` | 1024×768 | 会议结果阅读区与上下文操作栏 |

## Stitch 页面内容

以下内容是设计目标，不是自由发挥的占位文本。画板中必须保留关键事实、按钮和异常说明。

### S00 — Design System

目标：先锁定可复用系统，再开始页面设计。

画板需要展示：

- 浅色和深色背景、普通表面、抬升表面、边界、主文字、次文字。
- 主操作、次操作、幽灵、危险按钮的默认、按下、禁用、加载状态。
- 输入框、Radio、Accordion、Tabs、Badge、Progress、Alert、Dialog。
- `AppStatusNotice` 的信息、录音、警告、错误、成功五种语义。
- 会议列表条目、模型选项、转录片段、总结证据、音频控制条。
- 4、8、12、16、24、32、48 的间距标尺。
- 48×48 最小触控区域。
- 100–140 ms 按压反馈和 180–220 ms 内容切换标注。

组件细节：

- 卡片圆角 10 px；按钮和输入框圆角 8 px；Dialog 圆角 12 px。
- 边界使用 1 px 规则线；主要依靠间距、边界和表面亮度形成层级。
- 普通按钮不可使用胶囊形状。
- 图标采用 Lucide 风格，20 或 24 px，统一描边重量。
- 按下状态轻微缩放，禁止明显弹跳和发光。

### S01 — 会议列表

用户目标：快速开始新会议，或打开已有会议查看状态。

顶部：

- 页面标题：“会议”
- 右侧设置图标，语义为“打开设置”

列表样例：

1. “产品 Alpha 评审”
   - 状态：“录音中”
   - 时间：“2026年7月25日 09:30”
   - 时长：“14:28”
   - 使用红色录音图标和文字，不使用只有颜色的状态点。
2. “每周研究进展同步”
   - 状态：“已完成”
   - 时间：“2026年7月24日 15:00”
   - 时长：“42:08”
3. “离线转录恢复检查”
   - 状态：“失败”
   - 时间：“2026年7月23日 11:00”
   - 时长：“18:00”
   - 辅助文案：“打开会议查看失败原因”

主操作：

- 底部安全区固定文本按钮：“开始会议”
- 不设计只有 `+` 图标的浮动按钮。

布局要求：

- 手机使用单列、接近列表条目的扁平卡片，不做三张等权功能卡。
- 卡片标题最多两行；状态、日期和时长保持稳定位置。
- 点击整行，不在每个条目上堆叠多个快捷操作。
- 列表滚动时顶部标题保持简洁，底部主操作不覆盖最后一条会议。

### S02 — 会议列表空白

内容：

- 标题：“还没有会议”
- 说明：“开始第一场会议后，本地录音、最终转录和总结会显示在这里。”
- 主操作：“开始会议”
- 次级入口：右上角设置图标

不使用插画、彩色大图标或庆祝性动画。空白状态由“当前事实、用途说明、单一下一步”
组成，视觉中心略高于页面中线。

### S03 — 开始会议

顶部：

- 返回按钮
- 页面标题：“开始会议”

首个状态说明：

- 标题：“事实音频优先保存在本机”
- 正文：“实时转录变慢或失败时，录音仍会继续；结束后再基于完整音频处理。”

表单：

- 输入标签：“会议标题”
- Placeholder：“例如：产品周会”
- 辅助文案：“不填写时保存为‘未命名会议’。”

模型选择：

- 默认折叠标题：“转录模型：标准模型（Paraformer）”
- 辅助信息：“使用全局默认 · 已安装 · 开始后不可切换”
- 展开后显示：
  - “标准模型（Paraformer）”
  - “默认、低功耗，适合普通话与英语 · 已安装”
  - “高级模型（Qwen3-ASR）”
  - “准确率优先，资源占用较高 · 未下载”

底部主操作：

- “开始录音”
- 准备中替换为：“正在准备录音”
- 辅助文案：“开始前会检查麦克风权限、存储空间和模型完整性。”

交互要求：

- 模型选择是次级内容，不得比会议标题和开始按钮醒目。
- 点击开始后立即显示按压反馈，不等待反馈动画结束才执行。
- 防止重复点击，但禁用状态必须通过旁边文案说明原因。

### S04 — 高级模型尚不可用

沿用 `S03` 页面结构，模型选择为高级模型。

警告：

- 标题：“高级模型资源占用较高”
- 正文：“准确率优先，但会增加内存、耗电和发热；开始后本场不可切换。”

未下载状态：

- 标题：“高级模型尚不可用”
- 正文：“完成下载和校验后再开始，或明确改用标准模型并开始。”
- 资源说明：“约 941 MB · 至少需要 2 GB 空间 · 建议使用 Wi-Fi”

操作顺序：

1. 主操作：“下载高级模型”
2. 次操作：“改用标准模型并开始”
3. 文字操作：“取消本次选择”

不得自动切换到标准模型，也不得在用户不知情时开始录音。

### S05 — 录音工作台

这是整个产品最重要的画板。

顶部：

- 页面标题：“会议录音”
- 录音中不提供普通返回；系统返回进入 `S07` 的结束确认。

事实区：

- 会议名称：“产品 Alpha 评审”
- 大号等宽计时：“00:14:28”
- 锁定信息：“标准模型（Paraformer） · 本场已锁定”

第一状态通知：

- 标题：“事实音频正在安全写入”
- 正文：“锁屏或切到后台后录音仍会继续。”
- 使用红色录音图标，但通知背景保持克制。

第二状态通知：

- 标题：“实时转录正常”
- 正文：“当前没有待处理音频。”

实时转录：

- 区段标题：“实时转录”
- 说明：“仅供会中参考，结束后会基于本场锁定模型生成最终转录。”
- 示例：
  - `12:05` “本周先完成 Android Alpha 的会议结果页和端侧回归。”
  - `13:01` “高级模型继续按需下载，标准模型保持内置。”
  - `14:12` “录音必须独立于实时转录持续写入。”

底部操作：

- 次操作：“暂停录音”
- 主操作：“结束并保存”
- 辅助文案：“结束前录音会持续写入本机；实时转录变慢不会影响事实音频。”

视觉要求：

- 计时、事实音频状态和结束操作必须在首屏。
- 不设计跳动波形、呼吸背景或持续扩散圆环。
- 红色仅用于录音状态和结束语义，不将整屏染红。
- 新转录只轻微淡入，不推动事实区和操作区的位置。

### S06 — 录音降级

沿用 `S05` 的空间结构，事实区和按钮位置完全不变。

积压状态：

- 警告标题：“实时转录积压，录音仍在继续”
- 正文：“事实音频不受影响，系统正在追赶 6800 毫秒转录。”
- 可显示小型指标：“待处理 4.2 秒”

仅录音状态变体：

- 警告标题：“实时转录已停止，录音仍在继续”
- 正文：“事实音频仍在安全写入；结束后可基于完整音频继续处理。”
- 转录空白文案：“实时转录已停止，录音仍在继续。”

设计必须让用户第一眼确认“录音仍在继续”，不能只突出 ASR 错误。

### S07 — 结束并保存确认

在 `S05` 上方展示居中 Dialog，背景轻微变暗但仍能看到计时和录音状态。

内容：

- 标题：“结束并保存会议？”
- 正文：“结束后会先封存本机事实音频，再使用本场锁定模型生成最终转录。”
- 次操作：“继续录音”
- 主操作：“结束并保存”

动效标注：

- Dialog 从中心淡入并从约 0.96 缩放到 1.0。
- 200–240 ms `ease-out`；关闭比打开快。
- 不从结束按钮位置弹出，因为它是模态确认，不是 Popover。

### S08 — 最终处理

顶部：

- 返回会议列表的导航保持可用。
- 会议名称：“产品 Alpha 评审”
- 辅助标题：“正在整理会议结果”

安全状态：

- 标题：“正在生成最终转录”
- 正文：“录音已经安全保存在本机。处理变慢或失败都不会影响事实音频。”

处理阶段：

1. “最终转录”
   - 状态：进行中
   - 说明：“使用本场锁定模型完整读取事实录音”
2. “说话人整理”
   - 状态：等待
   - 说明：“最终转录完成后尝试区分说话人”
3. “AI 总结”
   - 状态：等待用户操作
   - 说明：“最终转录完成后可由你主动生成”

底部信息：

- “来源模型：标准模型（Paraformer）”

不显示伪造的百分比或预计完成时间。进行中阶段可使用快速但克制的真实加载器。

### S09 — 会议结果·总结

顶部：

- 页面标题：“会议详情”
- 会议名称：“产品 Alpha 评审”
- 元信息：“标准模型（Paraformer） · 42:08 · 2026年7月26日”

页签顺序：

- “总结”
- “转录”
- “录音”

当总结存在时默认打开“总结”；总结不可用时默认打开“转录”。

总结内容：

- 区段：“概览”
- 正文：“会议确认了 Android Alpha 结果页、端侧回归和双模型交付顺序。”
- 区段：“关键结论”
  1. “先完成结果页与端侧回归。”
     - 证据按钮：“02:05–02:17”
  2. “标准模型保持内置，高级模型继续按需下载。”
     - 证据按钮：“03:01–03:16”
- 区段：“行动项”
  1. “完成 Android Alpha 结果页回归。”
  2. “在同一设备上完成双模型性能对比。”

状态说明：

- “AI 总结仅基于当前最终转录生成。”
- 关键结论的证据按钮必须明确可点击，点击后定位并播放本地事实音频。

布局要求：

- 使用连续阅读文档，不把概览、每条结论、每个行动项分别包成嵌套卡片。
- 页签切换保持标题和元信息位置稳定。
- 证据时间戳使用等宽数字。

### S10 — 会议结果·转录

顶部结构与 `S09` 相同，选中“转录”。

区段标题：

- “最终转录”
- 说明：“由本场锁定模型生成，可进入编辑模式修订文本和说话人。”
- 右侧操作：“编辑转录”

转录样例：

1. `02:05–02:17`
   - 说话人：“陈工”
   - 内容：“本周先完成 Android Alpha 的会议结果页和端侧回归。”
   - 标签：“证据定位”
2. `03:01–03:16`
   - 说话人：“林同学”
   - 内容：“高级模型继续按需下载，标准模型保持内置并作为默认选择。”
3. `04:20–04:32`
   - 说话人：“陈工”
   - 内容：“AI 总结只能基于最终转录，并保留带时间戳的原文证据。”

下方说话人区：

- 标题：“说话人”
- 开关：“自动说话人分离”
- 说明：“这是可降级能力；不可用时仍可手工标注。”
- 当前标签：“陈工 · 2 个片段”“林同学 · 1 个片段”

阅读模式不显示输入框。长转录保持清晰行高和稳定时间戳列。

### S11 — 转录编辑与说话人

沿用 `S10` 页面，进入显式编辑模式。

顶部操作：

- “取消编辑”

每个片段：

- 输入标签：“说话人”
- 输入标签：“转录内容”
- 时间戳和“证据定位”保持只读。

片段示例：

- 说话人输入：“陈工”
- 转录输入：“本周先完成 Android Alpha 的会议结果页和端侧回归。”

底部固定操作：

- 主操作：“保存转录修订”
- 辅助说明：“保存会生成新的最终转录版本；现有事实音频不会改变。”

人工说话人标签：

- 标题：“人工标签”
- 输入 Placeholder：“输入说话人名称”
- 辅助标签：“2 个片段”
- 操作：“保存标签”

保存中按钮在原位置切换为加载状态，不用突然替换成另一套布局。保存成功后安静回到
阅读模式；如果已有 AI 总结过期，在原位置显示明确提示。

### S12 — 会议结果·录音与数据

顶部结构与 `S09` 相同，选中“录音”。

本地录音：

- 标题：“本地录音”
- 说明：“这是本场会议的唯一事实源，播放和证据定位均使用这份本机音频。”
- 播放进度：“00:00 / 42:08”
- 主操作：“播放完整录音”
- 播放中替换为：“停止播放”
- 使用真实进度轨道，不生成装饰性伪波形。

结果与数据：

- 输入标签：“会议名称”
- 当前值：“产品 Alpha 评审”
- 操作：“保存会议名称”
- 分享操作：“分享纯文本”“分享 Markdown”

重新转录：

- 说明：“重新转录会生成独立快照；成功后才替换当前结果。”
- 模型选项：“标准模型（Paraformer）”“高级模型（Qwen3-ASR）”
- 操作：“使用所选模型重新转录”

危险区：

- 标题：“删除会议数据”
- 操作：“删除本场会议”
- 确认标题：“确认永久删除？”
- 确认说明：“将删除本场录音、转录、总结和处理记录，无法撤销。”
- 危险操作：“确认删除全部数据”
- 次操作：“取消”

危险区与分享、重转录之间必须有明显间距和边界，不能放在同一组普通按钮中。

### S13 — 设置·转录模型

顶部：

- 返回按钮
- 页面标题：“设置”

区段：

- 标题：“转录模型”
- 说明：“这里的选择只影响后续新会议，历史会议不会改变。”

标准模型：

- 名称：“标准模型（Paraformer）”
- 定位：“默认、低功耗，适合普通话与英语”
- 状态：“已安装”
- Radio：已选中

高级模型：

- 名称：“高级模型（Qwen3-ASR）”
- 定位：“准确率优先，资源占用较高”
- 状态：“已安装”
- 资源：“约 941 MB”
- 操作：“设为默认”“删除高级模型”

删除高级模型不能设计成普通蓝色主操作。设置页是分组列表，不做两个巨大的营销卡片。

### S14 — 设置·模型下载状态

沿用 `S13`，分别设计三个状态变体，可放在同一状态画板：

1. 下载中
   - 状态：“下载中”
   - 进度：“387 MB / 941 MB”
   - 操作：“取消下载”
2. 校验中
   - 状态：“校验中”
   - 说明：“正在校验模型文件完整性”
   - 操作禁用，并说明原因
3. 空间不足
   - 状态：“空间不足”
   - 说明：“高级模型至少需要 2 GB 可用空间，当前设备空间不足。”
   - 操作：“释放空间后重试”

下载和校验状态切换不移动模型名称、描述和状态标签的位置。

### S15 — 设置·存储与隐私

区段标题：

- “存储与隐私”
- 说明：“会议录音、转录与模型只保存在本机；卸载应用可能永久删除这些数据。”

用量示例：

- “会议数据：382.4 MiB”
- “模型数据：1.02 GiB”
- “数据库：3.8 MiB”
- “应用总计：1.40 GiB”
- “设备可用：28.6 GiB”

诊断：

- 说明：“诊断信息仅包含状态计数、存储指标、模型版本和错误码；不包含会议标题、
  转录、音频或本地路径。”
- 操作：“查看并分享诊断信息”

不得设计“上传全部数据”“云端备份”或账号入口。

### S16 — 状态组件总览

在同一画板展示以下真实状态，用于保证所有页面的一致性：

- Loading：“正在加载会议”
- Empty：“还没有会议”
- Error：
  - 标题：“会议加载失败”
  - 正文说明错误、本地事实音频状态和下一步
  - 操作：“重新加载”
- Processing：“正在生成最终转录”
- Success：“事实音频已保存”
- Disabled：“正在检查模型，暂时无法开始录音”
- Recording warning：“实时转录积压，录音仍在继续”
- Recording-only：“实时转录已停止，录音仍在继续”

所有状态同时使用图标、标题和文字，不得仅用颜色或无文案的 Badge。

## Stitch 宽屏布局

### T01 — 会议列表 1024

- 最大工作台宽度 1200，左右留白均衡。
- 会议列表使用两列，每列保持至少 320 px。
- “开始会议”保留明确文本，可位于标题栏右侧；不使用浮动圆形按钮。
- 空白和错误状态仍保持阅读宽度，不铺满整个窗口。

### T02 — 录音工作台 1024

- 左侧约 40%：会议名称、计时、模型锁定、事实音频状态和录音操作。
- 右侧约 60%：实时转录。
- 左侧事实区不随右侧转录滚动。
- “暂停录音”和“结束并保存”固定在左侧安全区域内。
- 降级状态只更新提示，不改变双栏结构。

### T03 — 会议结果 1024

- 主阅读区约 65%：总结、转录或音频内容。
- 上下文操作区约 35%：会议元信息、分享、重新转录。
- 危险删除操作固定在上下文栏底部并与普通操作分隔。
- 主阅读行宽保持舒适，不将转录文本铺满窗口。

## Stitch 交付检查

Stitch 输出必须满足：

- `S00`～`S16` 和 `T01`～`T03` 使用同一设计系统。
- 主流程画板可以按顺序连接：列表 → 开始 → 录音 → 结束确认 → 处理 → 结果。
- 每个关键画板包含真实中文内容和真实产品状态。
- 页面没有登录、账号、云同步、AI 对话或底部导航。
- 没有渐变、玻璃拟态、装饰性波形、嵌套卡片或大面积品牌色。
- 录音页的事实音频状态、计时和结束操作在首屏可见。
- ASR 降级状态明确显示“录音仍在继续”。
- 结果页证据时间戳明显可操作。
- 危险删除区与普通操作分开。
- 提供 `S05`、`S09`、`S13` 的深色画板。
- 画板标注按压、内容切换、Dialog 和 reduced-motion 行为。

## 中文文案

- 中文优先；`AI`、`Markdown`、`Paraformer`、`Qwen3-ASR` 等必要名称保留。
- 按钮使用明确动词，例如“开始录音”“保存转录”“重试下载”。
- 不使用“确定”“提交”“出了点问题”等模糊文案。
- 关键文案必须说明事实音频是否安全。

## 禁止模式

- 卡片套卡片、三列等宽功能卡、全页居中、渐变标题、玻璃拟态。
- 用大面积品牌色制造层级。
- 不可点击的装饰性状态胶囊。
- 只靠颜色表达录音、错误、积压或完成。
- 所有区段使用相同内边距。
- 功能页面硬编码颜色、圆角、字体、断点和重复间距。

## 质量门槛

- 使用真实 `Application`/`FTheme` 编写组件测试。
- 覆盖浅色、深色、字体缩放、语义、最小触控和响应式尺寸。
- 完整用户流覆盖列表 → 开始 → 录音 → 处理 → 结果。
- 交付前运行 `dart format lib test integration_test`、`flutter analyze`、
  `flutter test` 和支持的 Android UI 测试。

## 当前 Flutter 实施基线

下表只记录 Stitch 设计开始前的代码基线，不限制新画板的视觉层级和组件拆分。
Stitch 方案确认后，应重新建立 Design Engineering 迁移状态，不把下表视为新版设计已实现。

| 阶段 | 状态 | 当前结果 |
|---|---|---|
| UI-00 视觉系统 | 已完成 | Cobalt 色彩、`AppColors`、`AppStyle`、响应式断点与本设计源已建立 |
| UI-01 共享组件 | 已完成 | 页面骨架、状态面板、状态通知及浅色/深色/窄屏预览已建立 |
| UI-02 会议入口 | 已完成 | 会议列表、会议状态、底部文本主操作、开始会议表单与高级模型降级路径已统一 |
| UI-03 录音工作台 | 已完成 | 事实音频与计时优先、模型锁定、积压/仅录音降级、统一结束确认及 375/414/1024 px 预览已建立 |
| UI-04 处理与结果 | 已完成 | 诚实最终处理阶段、转录默认阅读、总结/转录/录音三视图、显式编辑模式及本地证据定位播放已建立 |
| UI-05 设置与数据控制 | 待实施 | 默认模型、高级模型、存储隐私和诊断导出 |
| UI-06 全流程验证 | 待实施 | 组件预览、Android UI 测试、截图与 320～1024 px 验收 |
