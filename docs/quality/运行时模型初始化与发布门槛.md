# 会迹（MeetTrace）运行时模型初始化与发布门槛

> 更新日期：2026-08-06
>
> 状态：活动；运行时供应链、公开 API 分离生产装配、联合快照、音频分享仓库实现与双端应用标识已完成；官方 Dart 绑定内存缺陷按已接受风险跟踪，阈值校准、正式签名和双平台真机证据仍阻塞扩大分发

## 固定资产

| 资源 | 下载大小 | 当前状态 |
| --- | ---: | --- |
| SenseVoice `model.int8.onnx` | 239,233,841 B | Manifest 已存在 |
| SenseVoice `tokens.txt` | 315,894 B | Manifest 已存在 |
| Silero VAD | 212,860 B | Manifest 已存在 |
| Pyannote 分段模型官方归档 | 6,958,444 B | Manifest、MIT 许可、受限解包与目标镜像已完成 |
| 3D-Speaker 嵌入模型 | 39,593,761 B | Manifest、Apache-2.0 许可、安装与目标镜像已完成 |
| **全部运行时下载合计** | **286,314,800 B** | 低于上限 |
| **硬上限** | **300,000,000 B** | 不得静默提高 |

SenseVoice 使用不可变 revision `2365baeacb507f821a0c8120fcee3d484dba7a07`。Pyannote 归档 SHA-256 为 `24615ee884c897d9d2ba09bb4d30da6bb1b15e685065962db5b02e76e4996488`，其中目标 `model.int8.onnx` 为 1,540,506 B、SHA-256 为 `d582f4b4c6b48205de7e0643c57df0df5615a3c176189be3fc461e9d18827b5d`。3D-Speaker 权重 SHA-256 为 `1a331345f04805badbb495c775a6ddffcdd1a732567d5ec8b3d5749e3c7a5e4b`。精确 URL、安装文件集和许可要求见技术方案；初始化可用空间门槛固定为 `1,073,741,824` B。

## 当前实现差距

- 当前代码已统一初始化 SenseVoice、Silero VAD、Pyannote 与 3D-Speaker，并完成官方 `OfflineSpeakerDiarization` Dart API 的独立 worker/isolate 适配器。
- 两份说话人资源已发布到 `mt.zhangheng.eu.org/models/SpeakerDiarization/`；2026-08-03 公网验收确认 200、正确大小、ETag/本地 MD5 一致和 Range 206。
- 官方 `sherpa_onnx 1.13.4` 与上游 `master` 的 Dart `process*` 未释放完整波形原生缓冲区；30 分钟 16 kHz 音频单次至少遗留 `115,200,000` B。2026-08-05 产品决策接受 Alpha 的该项风险，Debug/Release 生产组合根均启用公开 API 适配器；所有会议默认开启、不设会议时长上限、不显示前置风险提示，用户可用全局开关关闭后续会议分离。仍不得用私有 API 或自建 FFI 绕过。自动化覆盖配置、初始化/推理失败、初始化期间取消、超时终止、空结果、释放和重复任务；threshold `0.5` 仍是待真机语料校准的工程值。
- AI 总结全链已删除，确定性标题、schema 7、数据代 3、联合最终快照与独立音频分享主链已完成；Android 插件缓存清理及模拟器取消/冷启动复验已通过，仍需 Android/iOS 真机完成接收端播放与全路径清理验证。
- Release 默认启用 Sentry 完整遥测；交付前必须验证 PII/采样率、Replay/截图/View Hierarchy 全量遮罩、录音期高开销遥测 Gate、符号化和 SDK 失败不阻断事实录音。
- Android/iOS 生产标识已统一为 `com.meettrace.app`；Android 正式签名 Environment 尚未配置，iOS `testflight` Environment 已完成一次签名上传。
- 旧的格式化、分析、测试、APK 与 OCR 结果仅证明旧代码快照，V1.0 发布自动化完成后必须全部重跑。

## V1.0 仓库门禁

- [x] 新增固定说话人 Manifest，并验证全部下载量严格等于 `286,314,800` B 且不超过十进制 300 MB。
- [x] 实现 1 GiB 空间预检、移动网络同意资源集合失效、Range 续传、暂停、严格校验和全部资源原子激活。
- [x] Pyannote 归档解包拒绝绝对路径、路径穿越、符号链接和白名单外文件；激活后不保留下载归档或浮点模型。
- [x] `mt.zhangheng.eu.org` 上两份说话人镜像返回 200、支持 Range，并与 Manifest 的大小和本地校验值完全一致。
- [x] 通过官方 `sherpa_onnx` Dart API 实现独立 worker 离线说话人分离并以 `numClusters=-1` 自动估计人数。
- [x] Debug/Release 生产组合根启用真实分离服务，默认偏好开启且关闭后不会创建最终分离任务。
- [ ] 升级到已修复完整波形原生缓冲区释放的官方包版本，并通过 30 分钟重复分离内存复验；该项为风险消除与扩大正式分发门槛，不再单独阻断 Alpha 构建启用。
- [ ] 校准并锁定聚类 threshold，完成普通话 2/3/4 人目标真机质量与性能证据。
- [x] `FinalResultCoordinator` 从同一事实音频并行运行最终 ASR 与分离，两条任务结束后只通过一次 CAS 事务发布联合快照。
- [x] 分离失败、超时、空结果或能力关闭生成单一说话人降级结果；ASR 失败终止分离并保留旧活动快照。
- [x] 删除 AI 总结专属 Port、Use Case、Service、Repository、schema、任务、UI 与测试，不保留隐藏入口。
- [x] 实现确定性会议标题、基于新快照 CAS 的手工说话人标签修订和文本分享。
- [x] 实现二次确认的临时 WAV 音频分享、精确空间差额、应用自有临时目录与 Android `share_plus` 插件缓存的全结果清理及启动恢复清理。
- [x] 提升 Alpha 数据代并验证旧数据库、音频、模型、检查点、偏好和分享临时文件均被全清。
- [x] Android `applicationId`、namespace 与 iOS Bundle ID 统一为 `com.meettrace.app`，并由配置守卫防止占位标识回归。
- [x] 当前 Android Debug APK 审计确认没有任何 ASR、VAD、Pyannote、3D-Speaker 权重、会议音频、转录或临时分享文件；IPA 仍待 macOS/iOS 阶段审计。
- [x] Pyannote MIT 许可和 3D-Speaker 精确权重 Apache-2.0 许可/NOTICE 已确认并进入安装包。
- [x] `dart format lib test integration_test`、`flutter analyze`、`flutter test` 和 Android Debug 构建基于当前代码重新通过。
- [x] OCR 审查全部 reviewable 文件，未解决 Critical/High 为零；修改代码后已更新 Graphify。

## 2026-08-03 仓库证据

- Windows 环境完成格式化与 `flutter analyze`，无静态分析问题；`flutter test` 共 358 项通过。
- `flutter build apk --debug` 成功，产物 `app-debug.apk` 为 278,613,944 B；KGP 仅保留 `flutter_foreground_task` 的未来兼容性告警。
- APK 检查确认 arm64 ABI 与 Flutter NOTICE 存在，说话人 Manifest、MIT/Apache-2.0 许可和 NOTICE 齐全；模型权重、归档、token 与用户数据命中数均为 0。
- OCR workspace 模式覆盖 76 个 reviewable 文件，排除 `.qoder/**`、`graphify-out/**`、`.impeccable/**` 与构建检查结果；发现的官方绑定泄漏和 worker 退出竞态均已通过生产阻断及生命周期修复处理，未解决的仓库 Critical/High 为零。Graphify 更新后为 6,145 个节点、7,695 条边。

## 2026-08-04 阶段 4 增量证据

- `FinalResultCoordinator` 专项测试 12 项通过，覆盖并行启动、发布屏障、成功映射、能力降级、不可取消实现拒绝、ASR 失败取消、旧快照保留、同会议串行和 CAS 冲突。
- `flutter analyze` 无问题；`flutter test` 共 362 项通过。
- OCR 增量复查覆盖本阶段 7 个代码与测试文件，发现“可用但不可取消的分离实现可能滞留后台推理”生命周期风险；现已在启动推理前硬性校验生命周期能力并补充回归测试，未解决 Critical/High 为零。
- 本轮未重建 APK；安装包和权重审计仍沿用 2026-08-03 证据，阶段 5～7 完成后必须重新构建并覆盖旧结论。

## 2026-08-04 阶段 5 增量证据

- 独立音频分享专项测试覆盖 WAV 头与完整 PCM、RIFF 32 位上限、分享完成/取消/结果不可判定/异常清理、确认后二次空间校验、44 B 精确差额、同会议并发隔离、源 PCM 不变和启动残留恢复。
- 详情页组件测试使用真实 `Application`/`FTheme` 外壳，验证会议名、`00:02` 时长、`31.3 KiB` WAV 大小、敏感信息提醒、确认前零分享调用、确认后独立调用，以及空间不足时不出现确认操作。
- `flutter analyze` 无问题；`flutter test` 共 372 项通过。OCR 增量复查发现并修复并发临时文件命名碰撞与 RIFF 大小上限边界，未解决 Critical/High 为零。
- 本轮未重建 APK，且 Android/iOS 系统分享接收端播放与临时目录清理仍需真机留证；仓库发布结论保持 `blocked`。

## 2026-08-04 阶段 6 增量证据

- Android namespace、`applicationId` 与 `MainActivity` 包路径，以及 iOS Runner 的 Debug/Release/Profile Bundle ID 已统一为 `com.meettrace.app`；RunnerTests 使用 `com.meettrace.app.RunnerTests`。
- 应用标识守卫测试通过；`aapt dump badging` 确认新 Debug APK 包名为 `com.meettrace.app`、`minSdkVersion=24`、`targetSdkVersion=36`。
- `flutter build apk --debug` 成功，产物 `app-debug.apk` 为 248,539,500 B；重新审计确认 arm64 ABI、Flutter NOTICE、全部固定 Manifest/许可齐全，模型权重、归档、token、会议音频、转录数据库与分享临时文件命中数均为 0。
- `flutter analyze` 无问题；`flutter test` 共 373 项通过。OCR 增量审查覆盖 Android Gradle/Activity、iOS Xcode 配置、标识守卫与活动文档，未解决 Critical/High 为零。
- 当前产物仍是 Debug 签名，只能作为仓库构建证据；Android 受控正式签名、iOS 无签名构建/App bundle 审计、Apple Team 与 TestFlight 仍未完成。

## 2026-08-04 Android 分享缓存修复证据

- 分享适配器在打开系统面板前安装 Android 生命周期返回门：取消、不可用和异常路径立即清理；成功路径等待应用离开前台并恢复后清理；应用冷启动会恢复删除精确的 `cache/share_plus` 子目录。清理器不触碰临时根目录中的其他文件，也不跟随符号链接越界。
- Pixel 10 AVD（Android 16 / API 36 / x86_64）复验确认：面板打开期间 `meeting-audio.wav` 存在；取消返回后插件缓存与应用自有临时目录均为空；强停冷启动后仍无残留；事实源 `fact.pcm` 保持 `2,803,712` B 不变。
- `flutter analyze` 无问题；串行 `flutter test --concurrency=1` 共 383 项通过。OCR 增量审查覆盖分享服务、启动装配、ViewModel 及相应测试，修复成功路径提前清理竞态后，未解决 Critical/High 为零。
- `flutter build apk --debug` 成功，产物 `app-debug.apk` 为 `279,103,939` B，SHA-256 为 `A46F7D9EF48C7EB9DBC9638FA45379578966C9E23AC4F4EE5BCE1F3AEC182757`。解包审计确认 arm64-v8a、armeabi-v7a、x86_64、Flutter NOTICE、固定 Manifest 与许可齐全，模型权重、token、会议音频、转录和分享临时文件命中数均为 0；体积由三 ABI 原生库及 Debug 产物主导，不是运行时模型进入 APK。
- 模拟器仅闭环取消与冷启动恢复子项；Android arm64 与 iOS 真机仍需选择接收端、确认 WAV 可播放并复核完整清理，仓库发布结论保持 `blocked`。

## 2026-08-05 说话人分离生产启用证据

- Debug/Release 组合根均改为从固定 Manifest 与已校验模型路径创建 `SherpaOnnxSpeakerDiarizationService`；偏好无记录时默认开启，明确关闭后 `FinalResultCoordinator` 在创建分离任务前退出。专项测试覆盖默认开启、关闭持久化、关闭不建任务、偏好读取失败沿用默认开启和生产工厂参数映射。
- `flutter analyze` 无问题；`flutter test` 共 413 项通过。`flutter build apk --debug` 与 `flutter build apk --release` 均成功；Debug APK 为 `269,134,225` B、SHA-256 为 `63EBCBC3A97CBC57D768C7BF23BE73DB1F12CA133206808A025597B7F8AF62E4`，Release APK 为 `139,339,919` B、SHA-256 为 `9EC5D7E0E8EE7C48FD5AF755343B10D9DFF813DA0BF990460827D0B935EA8860`。
- OCR workspace 模式审查 9 个源码与测试文件，排除 `graphify-out/**`；未发现 Critical/High/Medium。Graphify 更新后为 5,192 个节点、7,029 条边，因超过 5,000 节点使用聚合 HTML；12 个 Manifest/配置源文件仍报告零节点告警。
- Android 16 模拟器以保留安装方式成功部署并启动 `com.meettrace.app`，进程存活且启动日志无 Fatal。Mi 10 的旧包已不在设备上，两次安装均被 MIUI 以 `INSTALL_FAILED_USER_RESTRICTED` 拒绝；需在设备上允许 USB 安装后恢复，真机功能验证尚未完成。

## Android 外部门禁

- [x] 修复 Android `share_plus` 插件缓存残留；Pixel 10 模拟器已确认取消返回及冷启动恢复后 `cache/share_plus` 与应用自有 `files/meettrace/share` 均无残留，面板打开期间文件不会被提前删除。
- [ ] Mi 10 当前未安装 `com.meettrace.app`；安装新 Debug APK 时 MIUI 返回 `INSTALL_FAILED_USER_RESTRICTED`，需用户在设备上允许 USB 安装后重试。
- [ ] Mi 10 完成首次 286.3 MB 下载、移动网络同意、暂停续传、修复、联合最终处理、标签修订和 WAV 分享冒烟。
- [ ] API 24 arm64 真机完成安装、权限、录音恢复、运行时模型创建与释放。
- [ ] 约 4 GB RAM arm64 真机完成 30 分钟录音、SenseVoice 指标、说话人 DER/人数误差/RTF、内存、电量和温控。
- [ ] `com.meettrace.app` 签名 `arm64-v8a` APK 进入当前仓库 Draft Release；与同 SHA 的 iOS TestFlight 候选验收后公开原 APK，升级按新数据代全清并重新初始化。

## iOS 外部门禁

- [x] 最新稳定 macOS/Xcode/Flutter 的 [无签名 run 31061231263](https://github.com/zhangheng2022/meet_trace/actions/runs/31061231263) 已完成 Debug/Release 构建、App bundle 审计、unsigned IPA 封装和 7 天证据上传。
- [x] `com.meettrace.app`、Apple Team、App Store profile 与 API Key 已通过 `testflight` Environment 完成 [TestFlight run 31063319355](https://github.com/zhangheng2022/meet_trace/actions/runs/31063319355)；该运行对应 commit `ff8c59be9c060cee96d422166aaca17a31df54ba`。
- [ ] 在固定 `docs/quality/alpha_release_input.json` 补齐 schema 4 证据；从 `master` 依次运行同一 release ID 的 Android Alpha 与 TestFlight 流程，由 annotated tag 自动锁定同一候选 SHA。
- [ ] iPhone/iPad 完成首次下载、后台录音、系统中断、联合最终处理、WAV 分享、无障碍和同等性能指标。

仓库技术检查失败时自动阻断发布。任一目标平台证据缺失时，本质量文档的结论仍记为 `blocked`，但该结论不再由 GitHub Actions 自动读取；最终是否接受风险公开 Alpha 由 `github-release` 批准人负责。

## 2026-08-06 GitHub 公开 Alpha 发布自动化证据

- PRD V1.0、统一发布规格和维护者 Runbook 已统一为：只手动运行 `Alpha Release`，自动串联技术检查、Android Draft、iOS TestFlight，并在 `github-release` 执行一次公开批准。
- Android Draft 阶段允许相同 release ID/tag/SHA 的恢复性重试和候选资产替换；公开后标签、APK 与双平台候选清单不可覆盖。最终流程强制唯一固定名称 APK、零 IPA，并校验大小与 SHA-256。
- `dart format --output=none --set-exit-if-changed lib test integration_test tool` 检查 274 个文件且无变化；`flutter analyze` 无问题；完整 `flutter test` 共 445 项通过；6 项发布工作流守卫测试通过。
- `actionlint 1.7.12` 校验全部 GitHub Actions 工作流无错误；OCR workspace 模式审查正式发布工作流和守卫测试，显式排除生成的 `graphify-out/**`，未发现未解决的 Critical/High。
- 以上只证明仓库自动化和静态合同，不等于真实发布成功。签名 Secrets、Environment reviewer 调整、Ruleset、目标真机验收和首次 Draft/Pre-release 云端运行仍未完成，质量记录继续为 `blocked`，由公开批准人决定是否接受风险。
- 统一 `Alpha Release` 工作流不要求人工填写 SHA、门禁路径或 run ID：首次运行自动锁定 `master` SHA，Draft 重跑从 Android annotated tag 恢复同一候选。`docs/quality/alpha_release_input.json` 仅用于非阻断质量记录，发布工作流不读取它。
