# 会迹（MeetTrace）文档索引

> 状态：当前活动文档入口
> 更新日期：2026-08-01

## 活动文档

1. [Android + iOS Alpha PRD V0.7](./会迹_MeetTrace_Alpha_PRD_无登录版.md)：产品范围、初始化下载决策和验收标准的唯一事实源。
2. [端侧 SenseVoice 转录技术方案](./端侧SenseVoice转录技术方案.md)：录音、初始化下载、校验、模型锁定、ASR/VAD 和存储设计。
3. [Codex Alpha 开发步骤](./Codex_Alpha_开发步骤.md)：本轮实施状态、验证命令和剩余外部设备门槛。
4. [SenseVoice 初始化下载与发布门槛](./quality/SenseVoice_初始化下载与发布门槛.md)：Manifest、包内容、自动化和真机证据清单。
5. [交互与视觉系统](../DESIGN.md)：页面结构和 UI 契约。
6. [Git 分支与 Worktree 约定](./Git_分支与_Worktree_约定.md)：仓库协作规则。

以下仍是当前功能链的专项证据：可靠录音与崩溃恢复、Silero VAD 与预览队列、会议主链、最终转录快照、说话人降级、AI 总结证据链、结果页与数据控制，以及 Android/iOS 设备矩阵。已被 SenseVoice 方案替换的 Paraformer/Qwen 双模型文档不在 `docs/` 保留副本，需从 Git 历史追溯。

## 文档优先级

```text
PRD（做什么、如何验收）
  → 技术方案（怎样设计）
    → 开发步骤（怎样交付）
      → 代码、自动化、真机与包审计（证据）
```

发生冲突时按上面的顺序处理，并同步修订下游文档。`AGENTS.md` 是执行约束，必须与活动文档一致。

## 当前实现状态

- 默认且唯一可选 ASR 为 `sherpa-onnx-sense-voice-zh-en-ja-ko-yue-int8-2024-07-17`。
- 其他模型待定；设置页保留真实模型选择器，但不显示占位模型、等级或“即将推出”。
- SenseVoice 和 Silero VAD 权重不进入安装包，首次初始化时下载；未就绪前不进入首页。
- 固定下载量为 `239,762,595` 字节，小于十进制 `300,000,000` 字节上限。
- Wi-Fi 自动下载；移动网络必须先展示体积与数据费用提示，并取得绑定当前资源版本集合的持久同意。
- 下载支持 HTTPS Range 续传。用户暂停后保留分片；下次继续时不重复已完成字节。
- 第二次启动仅做安装记录、固定文件集合和字节数的本地快速检查，不联网检查版本。缺失、损坏或 Engine 初始化失败进入修复流程。
- 会议创建时锁定模型 ID、版本、`language=auto` 和 `useInverseTextNormalization=true`，会中与最终转录共用，不自动切换或混合输出。
- Alpha 数据库采用干净安装策略。检测到旧 schema 时阻断升级并要求先导出录音；不会静默删除录音。

## 维护规则

- 改变 P0 或验收标准前先更新 PRD，并执行 `$grill-me`。
- 改变模型、Manifest、下载、锁定或降级策略时同步更新技术方案和质量证据。
- 下载模型、真实录音、评测语料、密钥、`build/` 和 `coverage/` 不得提交。
- 活动文档不保留旧方案并列副本；历史由 Git 保存。
